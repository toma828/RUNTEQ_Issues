<div id="quote" class="font-serif vertical-text text-3xl opacity-0">
    <p>知識とは経験である</p>
    <p class="mt-4">ーーアルディアス</p>
</div>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const tl = gsap.timeline();

        tl.to("#quote", {duration: 2, opacity: 1, ease: "power2.inOut"})
          .to("#quote", {duration: 2, opacity: 0, ease: "power2.inOut", delay: 1})
          .call(() => {
              window.location.href = '/top'; // TOPページへのリダイレクト
          });
    });
</script>